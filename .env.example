# X API configuration
# Keep real values in .env (never commit secrets)
X_API_BASE_URL=https://api.x.com/2
X_BEARER_TOKEN=

# Optional placeholders for future OAuth 1.0a flows
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
